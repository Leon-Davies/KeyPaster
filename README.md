# KeyPaster

KeyPaster is a small Windows utility for mapping a keyboard key to a reusable block of text. The application temporarily replaces the clipboard, pastes the mapped text, and restores the previous clipboard contents.

Development is in progress.
